prefix(p_attr, argu)
	$pfs = webkit moz ms o
	for $pf in $pfs
		-{$pf}-{p_attr} argu
	{p_attr} argu

box-shadow()
	prefix(box-shadow, arguments)

button 
	box-shadow 2px 1px 10px red

div
	box-shadow 2px 1px 24px red